### 问题分析
您遇到的问题（数据一直为0）是因为**数据库表结构没有更新**。
虽然我在代码中定义了新字段（`bid1_vol`, `ask1_vol`, `tick_vol`），但由于您的数据库文件 `market.db` 是旧版本创建的，SQLite 的 `CREATE TABLE IF NOT EXISTS` 语句**不会自动给旧表添加新列**。这导致后端在写入这些数据时，因为找不到列而失败或被忽略，读取时自然也就只有默认值0。

### 解决方案
我需要手动执行一次数据库迁移，给现有的 `sentiment_snapshots` 表加上这三个缺失的字段。

1.  **创建迁移脚本**：编写一个临时的 Python 脚本 `backend/migrate_db.py`，用于检测并添加缺失的列（`bid1_vol`, `ask1_vol`, `tick_vol`）。
2.  **执行迁移**：运行该脚本，升级数据库结构。
3.  **重启后端**：重启服务以确保新结构生效。

### 执行步骤
1.  创建并运行迁移脚本。
2.  重启后端服务。
3.  您再次刷新页面，数据即可正常显示。

我现在开始执行。