针对您提出的两个问题，我将执行以下排查和修复计划：

### 1. 情绪数据缺失（左下角空白）
*   **原因推测**：目前代码中腾讯接口的超时时间仅设置为 **3秒**。鉴于您提到的新浪数据加载需 10秒，推测您的网络环境连接此类金融接口较慢，导致腾讯请求直接超时失败。
*   **修复方案**：将 `fetch_tencent_snapshot` 的超时时间增加至 **10秒**，并增加详细的错误日志，确保数据能被正常获取。

### 2. 主力动态加载慢（~10秒）
*   **原因解释**：该图表依赖新浪的 **逐笔成交数据 (Level-1 Ticks)**。此接口机制是每次请求都下载**当天开盘至今的所有交易记录**。对于热门股票，如下午盘时可能包含数万条数据（数MB文本），下载+解析确实需要数秒至十余秒。这是免费高精度数据的固有成本。
*   **优化方案**：我将编写一个调试脚本，精确测试“网络下载”与“数据解析”各占用的时间。如果瓶颈在解析，我可以尝试优化算法；如果是网络原因，我会建议适当调大前端的自动刷新间隔（如改为 15秒）。

### 执行步骤
1.  创建 `debug_market.py` 脚本，基准测试 API 响应速度和数据格式。
2.  修改后端代码，放宽超时限制并增强健壮性。
3.  运行测试并汇报结果。