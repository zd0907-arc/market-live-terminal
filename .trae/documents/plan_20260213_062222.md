### 后端排查结果
我已经通过 `curl` 验证了后端接口 `/api/sentiment?symbol=sz000833` 的返回数据，结果显示 **数据完全正常**：
```json
{
  "bid1_vol": 270.0,
  "ask1_vol": 4.0,
  "tick_vol": 431345.0,
  "cvd": 85048.0,
  "oib": -647.0
}
```
这证明后端已经成功解析并返回了非零数据。

### 前端问题定位
既然后端返回了数据，但您页面上显示的还是 0，那么问题一定出在 **前端组件 (`SentimentTrend.tsx`) 没有正确读取这些字段**。

我刚才回顾了前端代码，发现了一个可能的问题：
在 `fetchLive` 函数中，我们虽然把 `json.data` 解析并设置到了 `setLiveData`，但可能没有触发 React 的重新渲染，或者 `SentimentTrend` 组件内部在某些情况下使用了默认值。

更重要的是，我之前的代码修改中，`SentimentTrend.tsx` 的接口定义 `SentimentPoint` 虽然加了 `bid1_vol` 等字段，但在 `fetchLive` 的回调里：
```typescript
const livePoint: SentimentPoint = {
    timestamp: raw.timestamp,
    cvd: raw.cvd,
    oib: raw.oib,
    price: raw.price,
    signals: typeof raw.signals === 'string' ? JSON.parse(raw.signals) : (raw.signals || []),
    // 关键点：这里可能漏掉了把 raw.bid1_vol 赋值给 livePoint.bid1_vol
};
```
如果这里漏了赋值，那么组件状态里这两个字段就是 `undefined`，渲染时就会回退到默认值 0。

### 修复计划
1.  **检查前端赋值逻辑**：查看 `SentimentTrend.tsx` 中 `fetchLive` 函数，确认是否将 `raw.bid1_vol` 等字段正确赋值给了 state。
2.  **修正代码**：如果确实漏了，我将补充赋值逻辑。
3.  **验证**：修改后，前端应该能立即显示后端返回的数值。

我现在立刻检查并修复前端代码。