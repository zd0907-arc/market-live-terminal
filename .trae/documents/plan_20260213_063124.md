好的，我将执行脏数据清理操作。

### 计划步骤
1.  **清理脏数据**：使用 `sqlite3` 命令删除 `sentiment_snapshots` 表中时间戳大于当前时间（例如大于 `15:00:00`，因为现在才下午2点半，且之前的脏数据是 16:14）的记录。更稳妥的方式是删除所有 `timestamp > '14:40:00'` 的今日数据。
2.  **验证清理结果**：再次查询最新几条数据，确保那条 `16:14` 的数据已消失，最新数据回归到 `14:30` 左右。
3.  **前端验证**：清理完成后，Tick 量的计算将自动恢复正常（因为对比基准变回了正常的上一条数据）。

我现在开始操作。