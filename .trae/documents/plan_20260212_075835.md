### 优化图表视觉样式

根据您的反馈，我将对累计趋势图进行以下调整：

1.  **主力净流入 (面积图)**:
    *   **去掉轮廓线**: 设置 `stroke="none"`，只保留红绿填充面积，视觉更纯粹。
2.  **主力累计买卖 (实线)**:
    *   **加深颜色**: 将透明度从 `0.3` 提升至 `0.8`，使其清晰可见。
3.  **新增：超大单累计买卖 (虚线)**:
    *   新增两条虚线 (`strokeDasharray="3 3"`) 分别展示 **超大单累计买入 (红)** 和 **超大单累计卖出 (绿)**。
    *   这两条线同样绑定到**右轴 (Total)**，方便与主力总额进行趋势对比。
4.  **超大单净流入 (紫线)**:
    *   保持现状，作为独立的趋势参考。

### 修改文件
*   `src/components/dashboard/RealtimeView.tsx`
