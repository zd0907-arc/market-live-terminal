收到，以后所有对话、系统界面和文档都将严格使用中文。

针对您“压缩头部高度、指标分两行放标题后、保持信息量不变”的要求，我将执行以下优化方案：

### 1. 界面布局重构 (`src/App.tsx`)
*   **布局调整**：将原本的“上下两行”结构改为“左右三段式”紧凑布局，大幅降低高度（预计减少 50%）。
    *   **左侧**：股票名称、代码、收藏按钮（第一行）；时间、状态（第二行）。
    *   **中间**（紧跟标题）：将指标分为两行展示。
        *   第一行：`成交: xxx` `金额: xxx`
        *   第二行：`高/低: x/x` `开/收: x/x`
    *   **右侧**：当前价格、涨跌幅。
*   **内边距压缩**：将卡片的内边距从 `p-6` 减少到 `p-3`，移除多余的 margin，使整体更扁平。

### 2. 中文标签本地化
*   将所有英文缩写改回中文：
    *   `Vol` -> `成交`
    *   `Amt` -> `额`
    *   `H/L` -> `高/低`
    *   `O/C` -> `开/收`
    *   `Core: Connected` -> `核心服务: 已连接`

### 3. 验证
*   确认修改后的界面高度显著降低。
*   确认所有信息（包括时间、状态、四个核心指标）都完整保留且为中文。
