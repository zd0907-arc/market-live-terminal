# 数据核对与验证计划

为了确保新浪历史数据源的准确性，我将执行以下验证步骤：

1.  **编写验证脚本 (`verify_data.py`)**：
    *   复用后端核心服务 (`backend.app.services.market`) 中的数据获取函数，确保验证环境与生产环境一致。
    *   针对您最近访问的股票列表：
        *   粤桂股份 (sz000833)
        *   天下秀 (sh600556)
        *   利通电子 (sh603629)
        *   天地在线 (sz002995)
        *   贝因美 (sz002570)
        *   网宿科技 (sz300017)
    *   拉取 **2026年2月11日** 的原始数据。

2.  **执行加工逻辑**：
    *   **主力定义**：超大单(r0) + 大单(r1)。
    *   **计算公式**：
        *   主力买入 = (r0买入 + r1买入)
        *   主力卖出 = (r0卖出 + r1卖出)
        *   净流入 = 主力买入 - 主力卖出
        *   活跃度 = (主力买入 + 主力卖出) / 总成交额
    *   脚本将输出每只股票的“原始字段”和“计算后指标”。

3.  **结果交付**：
    *   我将运行该脚本并向您展示输出结果。
    *   详细解释每个字段的来源和计算过程。

准备就绪，请批准执行。