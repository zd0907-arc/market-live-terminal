# 文档更新与代码提交计划

根据您的要求，我将把最近修复的 AI 摘要报错、时间显示问题以及新增的功能更新到文档中，并提交到 `release/v2.7.0` 分支。

## 1. 更新文档 (README.md)
在 `v2.7.0` 版本说明中补充以下修复和新特性：
*   **Fix**: 修复 AI 摘要功能因超时和运行时错误导致的 "Network Error"，并解决数据库表 (`sentiment_summaries`) 缺失问题。
*   **Fix**: 修正 AI 摘要生成的时间戳为本地时间 (Local Time)，解决 8 小时时差问题。
*   **Feature**: 顶部栏新增 **实时时钟 (RealTimeClock)**，替代原有的静态行情时间，提供秒级刷新。
*   **Feature**: 配置面板新增 **LLM 连通性测试** 功能，支持一键验证模型 API Key 配置是否正确。

## 2. 提交代码 (Git Commit)
将当前工作区的所有修改（包括新增文件）提交到当前分支 `release/v2.7.0`。

**涉及文件**:
*   **文档**: `README.md`
*   **后端**: `backend/app/routers/sentiment.py`, `backend/app/services/llm_service.py`, `backend/app/db/database.py`, `backend/app/routers/config.py` 等
*   **前端**: `src/components/common/RealTimeClock.tsx` (新文件), `src/App.tsx`, `src/services/sentimentService.ts`, `src/components/common/ConfigModal.tsx` 等

## 3. 执行步骤
1.  编辑 `README.md` 添加更新日志。
2.  运行 `git add .` 添加所有变动。
3.  运行 `git commit -m "fix: resolve AI summary errors, add real-time clock, and update docs"`。
