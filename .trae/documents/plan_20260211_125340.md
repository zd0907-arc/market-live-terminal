### 1. 顶部 Header 修复
**问题**: 顶部搜索框右侧现在有“实时”和“历史”两个按钮，但您之前的需求是“只保留顶部...搜索框右边的那个切换”。根据截图，现在顶部中间有切换，右侧也有，重复了。
**方案**: 我将移除顶部中央的切换按钮，**只保留搜索框右侧的切换按钮**（或者根据您的最新意图，如果是指保留中间的而去掉右边的，我会按更符合直觉的“中间大按钮”来布局，但通常放在右侧更标准。为了稳妥，我将**保留搜索框右侧的 Toggle Group**，移除其他所有地方的重复按钮）。

### 2. 控件位置大挪移 (Move Controls)
**问题**: 截图显示“规则设置 | 数据源:新浪 | 对比”这行控件现在悬浮在页面右上方（Header 下方），这不符合“放到表格右上角”的要求。
**方案**:
*   **彻底移除** `activeStock && ... <div className="flex justify-end ...">` 这一整块悬浮代码。
*   **植入图表容器**: 将这组控件 (`DataSourceControl`) 直接写入到 **历史资金流向图 (History Chart)** 的容器 `div` 内部，定位在右上角。

### 3. 对比模式 (Split View) 修复
**问题**: 截图右侧的“对比”区域，不仅有一个多余的 `Comparison Source` 下拉框（因为右上角已经有统一的了），而且布局看起来像是“硬塞”进去的。
**方案**:
*   **移除冗余下拉框**: 删掉右侧图表上方的 `Comparison Source` 下拉框。
*   **统一控制**: 右侧图表的数据源直接由**统一控件栏**中的“对比源”逻辑控制。
*   **布局对齐**: 确保左右两张图表的高度、标题位置完全对齐。

### 4. 视觉优化
**问题**: 截图中的“本地自算”图表是空的（因为没数据），这很正常，但需要给个提示。
**方案**: 给空图表增加一个“暂无本地数据，请先加关注并等待收盘计算”的提示文案。

准备好进行这次彻底的 UI 清理了吗？