# 02_BUSINESS_DOMAIN（系统需求总册 / Single Source of Truth）

> 核心定位：本文件是全系统业务需求的唯一真相源。每个能力以 `CAP-*` 卡片管理，统一记录“需求 + 业务规则 + 实现摘要 + 验收案例 + 可观测性 + 变更记录”。

## 0. 使用规则（强制）

- 任何业务行为变更，先在 `docs/changes/` 建立变更卡（规则见 `06_CHANGE_MANAGEMENT.md`）。
- 任何业务行为变更，必须先在对应 `CAP-*` 卡片补“拟变更点”。
- 改动完成后，必须回填同一卡片的“实现摘要 + 验收结果 + 变更记录（任务ID）”。
- 任务ID统一：`CHG-YYYYMMDD-序号`（示例：`CHG-20260309-02`）。
- 前后端 AI 与人工都以本文件为需求依据，不允许跨文档拼接需求。

## 1. 能力目录

| CAP ID | 能力名称 | 责任域 | 当前状态 |
|---|---|---|---|
| `CAP-MKT-TIME` | 交易日与交易时段展示状态机 | 前后端共享 | ACTIVE |
| `CAP-REALTIME-FLOW` | 主力动态实时数据链路 | 后端主责，前端联调 | ACTIVE |
| `CAP-HISTORY-30M` | 30分钟历史趋势与口径 | 后端主责 | ACTIVE |
| `CAP-WIN-PIPELINE` | Windows离线节点与云端注入 | 后端/运维 | ACTIVE（有阻塞） |
| `CAP-L2-HISTORY-FOUNDATION` | 盘后L2历史底座与回补机制 | 后端/前端/运维 | ACTIVE |
| `CAP-SANDBOX-REVIEW` | L1/L2 复盘沙盒验证闭环 | 后端/前端（沙盒） | ACTIVE |

## 2. CAP 卡片模板（新增需求时复制）

### CAP-XXXX（能力名称）
1. **业务目标**
2. **业务规则（强约束）**
   - 状态机/时间窗/口径定义（北京时间）
   - 数据来源优先级
   - 禁止行为（红线）
3. **实现摘要（当前方案）**
4. **验收案例（Given / When / Then）**
5. **可观测性与告警**
6. **变更记录（任务ID）**

---

## 3. 能力卡片

### CAP-MKT-TIME（交易日与交易时段展示状态机）

1. **业务目标**
- 在任意时刻都展示正确“交易状态 + 展示日期（display_date）”，避免误回溯、误前推、误复制。

2. **业务规则（强约束）**
- **时间基准**：北京时间（Asia/Shanghai）。
- **交易日状态机**：
  - `09:15-11:30`：上午交易态（实时模式，`display_date=当日`）
  - `11:30-13:00`：午休停盘态（非撮合，但仍属当日展示，`display_date=当日`）
  - `13:00-15:00`：下午交易态（实时模式，`display_date=当日`）
  - `15:00-24:00`：收盘态 / 盘后复盘（当日展示，不切到次日）
  - `00:00-09:15`：盘前态（展示最近交易日）
- **非交易日**：全天展示最近交易日。
- **前后端展示约束**：
  - 前端不得再自行仅凭浏览器本机时间输出最终“实时交易中/回溯模式”文案；
  - 最终展示必须以后端返回的 `market_status / market_status_label / default_display_scope / view_mode` 为准；
  - 在后端数据尚未返回前，前端允许展示“临时状态”，但临时状态语义必须与本 CAP 一致，不得出现盘后误报为“实时交易中”。
- **数据来源优先级**：交易日判断优先使用交易日历；失败时允许保守降级（不误判为交易进行中）。
- **禁止行为（红线）**：
  - 禁止将“前一交易日数据”复制成“自然日当天数据”；
  - 禁止非交易日写入“当天交易数据”；
  - 禁止在交易日盘后仍把状态显示成“实时交易中”。

3. **实现摘要（当前方案）**
- 后端 `MarketClock` 统一产出 `market_context`，明确给出：
  - `market_status / market_status_label`
  - `default_display_date`
  - `default_display_scope`
  - `should_use_realtime_path`
- 前端顶部状态 badge 不再自己猜最终状态，而是以后端权威字段为准；当数据尚未返回时，只允许显示与本 CAP 一致的临时状态。
- 交易日历由后端集中维护，失败时走保守策略，避免“假实时”。
- `/api/realtime/dashboard` 在“自然日当天且为交易日”时才允许走实时 ticks 聚合；若 `display_date` 回溯到上一交易日（周末/节假日/盘前），则优先走 `history_1m` 静态回放；若该日 `history_1m` 未准备好但 `trade_ticks` 已存在，则回退为“按该日 ticks 聚合”，避免周末页面空白。
- 对于“交易日当天但不在盘中（午休 / 盘后）”的场景，默认仍展示**今天**的数据，只是默认不再走“实时拉取路径”。

4. **验收案例（Given / When / Then）**
- **正常**：Given `2026-03-09 10:00`，When 打开实时页，Then `display_date=2026-03-09` 且非回溯模式。
- **边界**：Given `2026-03-09 12:00`（午休），When 打开实时页，Then 仍显示 `2026-03-09`，且不应被标记为历史回溯。
- **异常防线**：Given `2026-03-10 00:30`，When 请求实时接口，Then `display_date` 必须是最近交易日，且不得出现“复制成 2026-03-10 的盘中数据”。

5. **可观测性与告警**
- 观测点：`/api/realtime/dashboard` 返回的 `display_date`、前端“回溯模式”标签、后端时间判定日志。
- 异常信号：交易日盘中出现回溯标签，或非交易日出现当天交易数据。

6. **变更记录（任务ID）**
- `CHG-20260307-01`：修复凌晨/周末误写当日问题（避免前一日复制成今天）。
- `CHG-20260309-01`：补强时区口径一致性，修复盘中误回溯显示。
- `CHG-20260314-01`：修复周末“当日分时”空白问题；非交易日回溯显示上一交易日时，改走 `history_1m` 静态回放而非实时 ticks 聚合。
- `CHG-20260318-02`：把盯盘页市场状态收口为 `盘前 / 盘中 / 午间休市 / 盘后 / 休盘日` 五态；交易日盘后默认展示今日数据，前端最终状态以后端权威状态字段为准。

---

### CAP-REALTIME-FLOW（主力动态实时数据链路）

1. **业务目标**
- 在交易时段稳定展示“主力动态”（分钟级曲线 + 最新逐笔），即使用户未手动刷新也能持续有数据。

2. **业务规则（强约束）**
- **主力定义**：
  - 超大单：单笔成交金额 `>= 1,000,000`
  - 大单：单笔成交金额 `>= 200,000` 且 `< 1,000,000`
  - 主力资金 = 大单 + 超大单
- **前端刷新状态机**：
  - 仅保留二态：`盯盘开=5秒`、`盯盘关=30秒`；
  - 周期刷新必须采用“静默刷新”语义：请求失败时保留上一版成功数据，禁止先清空图表再等待回填。
- **活跃心跳分层**：
  - 实时页只对“当前正在查看的股票”注册心跳；
  - 心跳模式仅有 `focus` 与 `warm` 两层；
  - 历史日期查看（手动选择过去交易日）禁止继续注册实时心跳。
- **Windows 抓取节奏**：
  - `focus_symbols`：快照 `3秒`、tick `5秒`；
  - `warm_symbols`：tick `30秒`；
  - watchlist 兜底轮扫维持 `15分钟`；
  - 本轮明确不引入“最近查看股票”中频轮扫队列。
- **口径红线**：历史与实时统一使用“单笔金额阈值判定”，禁止因历史有订单号而单独启用拆单还原。
- **数据来源优先级**：
  1. 当日 `trade_ticks`（本地库）
  2. 本地采集任务拉取并覆盖写入
  3. 若用户在交易日当天（尤其盘后复盘）查看一只本地尚无分时的新股票，允许由 `/api/realtime/dashboard` 触发**按需补抓当天 full-day ticks**，再写入本地并聚合展示；
  4. 该“按需补抓”只适用于“今天”的查看语义，不得越权把历史日期伪造成今天。
- **成交方向兼容口径**：
  - 必须兼容 `买盘/卖盘/中性盘`、`buy/sell/neutral`、`B/S/M` 三类来源标记；
  - 对外统一标准化为 `buy/sell/neutral`，未知标记默认降级为 `neutral` 并打诊断日志。
- **禁止行为（红线）**：
  - 当日 `trade_ticks=0` 时静默显示“正常实时”；
  - 改阈值后不记录影响范围。

3. **实现摘要（当前方案）**
- 实时链路：采集（AkShare/Windows） -> `trade_ticks` 入库 -> 分钟聚合 -> `/api/realtime/dashboard` 输出。
- 聚合使用同一阈值配置，输出 `chart_data`、`cumulative_data`、`latest_ticks`。
- 分时与历史聚合共享成交方向归一化模块，避免因源字段枚举差异导致主力资金线归零。
- 实时页心跳已升级为 `focus/warm` 分层；Windows crawler 按 `focus=5秒 tick / warm=30秒 tick / watchlist=15分钟兜底` 执行。
- 前端实时页改为二态盯盘按钮，并在轮询时保留旧数据，避免“闪空白”。
- 当用户查看“今天”的分时但本地尚无该股票数据时，后端允许按需抓取 full-day ticks 并写回本地，再补 `history_1m` 聚合，避免盘后查看新股票时一直空白。

4. **验收案例（Given / When / Then）**
- **正常**：Given `2026-03-09 10:30` 且 watchlist 已采集，When 请求 `/api/realtime/dashboard?symbol=sz000833`，Then `chart_data` 非空且 `latest_ticks` 至少 1 条。
- **边界**：Given `2026-03-09 23:31` 当日 ticks 的 `type` 同时出现 `B/S/M` 与 `买盘/卖盘`，When 请求同一接口，Then 主力资金字段应保持非零且 `latest_ticks.type` 统一为 `buy/sell/neutral`。
- **边界**：Given 首次启动且尚未有当日 ticks，When 请求接口，Then 返回空数据并有可诊断信号（日志/监控），不得伪造曲线。
- **异常防线**：Given 采集器异常中断，When 连续请求接口，Then 能明确观测到 `trade_ticks` 未增长并触发排查路径。

5. **可观测性与告警**
- 观测点：`trade_ticks` 当日行数、`/api/realtime/dashboard` 数据长度、采集任务日志。
- 快速诊断：`trade_ticks(date=today)=0` 即判定为数据底座缺失，不先责怪前端图表。
- 快速诊断补充：若 `trade_ticks(date=today)` 未增长，优先检查 Windows 计划任务 `ZhangDataLiveCrawler` 的“最后运行时间 / 当前是否在跑”；`2026-03-16` 已验证这类故障会直接导致生产实时 K 线空白。
- 附加诊断：若 `latest_ticks.type` 出现未知枚举，需记录采样并评估是否补充归一化规则。

6. **变更记录（任务ID）**
- `CHG-20260307-02`：增强无人查看场景下的落数策略（周期全量轮扫 + 收盘重试）。
- `CHG-20260309-02`：本地快速补数恢复（watchlist 11 只股票当日 ticks 回填）。
- `CHG-20260309-09`：修复分时资金流方向枚举不兼容（`B/S/M` 与中文/英文口径统一归一化）。
- `CHG-20260310-01`：修复生产当日分时无数据（Windows 依赖缺失 + ingest 目标地址错误），恢复持续上报与实时分时展示。
- `CHG-20260312-02`：盯盘三态收敛为二态（5s/30s），实时页改为静默刷新，并将 heartbeat / crawler 升级为 `focus/warm` 分层抓取。
- `CHG-20260318-02`：新增“交易日当天但盘后查看新股票”按需补抓与聚合逻辑，并把实时页状态展示改为以后端权威状态为准。

---

### CAP-HISTORY-30M（30分钟历史趋势与口径）

1. **业务目标**
- 30分钟线稳定反映主力资金趋势，日内桶定义一致，历史与实时可解释。
- 截至 `2026-03-14` 晚间，产品目标已升级为：`30m` 不再作为新版盯盘页里的独立主 Tab，而是并入“历史多维”模块，与 `5m/1h/日` 统一切换。

2. **业务规则（强约束）**
- **标准桶**：每交易日固定 8 个 30m 桶（`09:30,10:00,10:30,11:00,13:00,13:30,14:00,14:30`）。
- **拟变更点（`CHG-20260315-01`）**：
  - 历史多维最终不只保留主图，还将扩展两张统一副图：
    1. 净流入对比；
    2. 买卖力度分离。
  - 两张副图都必须统一采用 `L1 vs L2`、`主力 vs 超大单` 的视觉语言，且对 `5m/30m/1h/日` 四个维度保持同构。
- **拟变更点（`CHG-20260315-03`）**：
  - 新版历史多维增加**轻量质量提示**，不建立复杂质量状态机；
  - 当前查看粒度下的每个点只允许返回：
    - `quality_info`：一段人类可读的问题说明；
    - `is_placeholder`：该点是否为查询层补出的缺失占位；
  - 前端统一使用一种异常样式，具体问题靠 tooltip 中的文字说明识别。
- **拟变更点（`CHG-20260316-08`）**：
  - 新版历史多维进入“专业终端交互重构”阶段：
    - 顶部 `[旧版 / 新版]` 切换上移到全局导航；
    - 历史多维图表头部收敛为单行极简控制区；
    - 统一支持 `5m/15m/30m/1h/日线` 粒度切换；
    - 保留 slider，但 inside 缩放需改为更适配 PC 触控板 / 手机长按的交互。
- **数据来源优先级**：
  1. `history_30m`（主力流向）
  2. 必要时由 ticks 重算（同口径）
- **禁止行为（红线）**：
  - 禁止把仅有OHLC、资金流为0的占位K线混写为真实资金流记录；
  - 禁止 8桶/10桶混口径对外展示。

3. **实现摘要（当前方案）**
- ETL、后端聚合、API读取统一 8 桶映射。
- API 层过滤非标准桶，backfill 默认禁用“零流入占位写入”路径。
- 截至 `2026-03-15`，新版历史多维第一版已落地：`5m/30m/1h/日` 共用一个主图容器，统一消费 `/api/history/multiframe`。
- 截至 `2026-03-15` 深夜，历史多维已新增两张副图第一版：
  - 净流入对比；
  - 买卖力度分离。
- 截至 `2026-03-16 01:10`，历史多维三张副图已统一改为“左超大 / 右主力”的真双柱渲染，深色 L2 包裹浅色 L1；tooltip 改为受边界约束的紧凑卡片布局，避免在屏幕边缘与卡片内部溢出。
- 截至 `2026-03-16 23:58`，新版历史多维已完成一轮 UI / 交互瘦身：
  - `[旧版 / 新版]` 切换上移到全局导航；
  - 历史多维头部收敛为单行控制区；
  - 新增 `15m` 粒度；
  - 主图开启稀疏时间轴标签；
  - 步进缩放、`Ctrl/⌘ + 滚轮` 缩放与移动端长按 tooltip 第一版已落地。
- 截至 `2026-03-17 00:38`，新版历史多维已完成第二轮交互 polish：
  - 大号跟随鼠标 tooltip 已退场，改为“外置顶栏读数条 + 十字线联动”；
  - y 轴保持左侧，仅将高/中/低三档刻度值内嵌到轴线右侧，提升图体有效宽度；
  - 价格元数据区改为“开盘价 / 最高价、最低价 / 收盘价、成交额”三行中文布局；
  - 普通滚轮优先回到页面滚动，图表仅保留 `Ctrl/⌘ + 滚轮` 缩放与 `Shift + 滚轮` 平移。
- 当前口径为前端基于 `/api/history/multiframe` 返回的 `buy/sell/total_amount` 直接派生，不新增后端契约；下一步主要是等 `2026-03` 正式数据齐全后做大样本校正。

4. **验收案例（Given / When / Then）**
- **正常**：Given `2026-02-13`，When 请求 `/api/history/trend?symbol=sz000833&days=20`，Then 当日应只出现 8 个标准时间桶。
- **边界**：Given 历史源存在 `11:30/15:00` 额外桶，When 入库或读取，Then 必须被归并/过滤，不得直接外显。
- **异常防线**：Given 出现“OHLC 非0但资金流全0”大面积记录，When 审计，Then 标记为污染窗口并进入重建流程。

5. **可观测性与告警**
- 观测点：`bar_count/day`、`zero_flow_ohlc_count`、关键票（如 `sz000833`）抽样比对。
- 告警条件：单日桶数不等于 8，或占位0流入比例异常升高。

6. **变更记录（任务ID）**
- `CHG-20260307-03`：30m 口径统一为 8 桶并上线（v4.2.8）。
- `CHG-20260308-01`：Windows ETL 按新口径重建 `202602` 数据（待回传merge完成最终闭环）。
- `CHG-20260314-08`：冻结新版盯盘页信息架构，`30m` 未来与 `5m/1h/日` 一并收敛到“历史多维”模块。
- `CHG-20260315-01`：冻结历史多维副图扩展方案，新增“净流入对比 + 买卖力度分离”两张统一副图设计。
- `CHG-20260315-03`：冻结新版历史多维轻量质量提示方案，统一使用 `quality_info + is_placeholder`，不扩散到老版页面。
- `CHG-20260316-08`：完成新版历史多维专业终端交互重构第一版，包含顶部版本切换迁移、15m 粒度、步进缩放、头部瘦身与移动端长按 tooltip。
- `CHG-20260317-09`：完成新版历史多维外置读数条与 y 轴布局修正，并发布 `v4.2.17`。

---

### CAP-WIN-PIPELINE（Windows离线节点与云端注入）

1. **业务目标**
- 利用 Windows 节点承担离线重建与盘中采集，向云端被动注入数据，保证在云端禁外采约束下系统可持续运行。

2. **业务规则（强约束）**
- **节点职责**：
  - Windows：采集/ETL 产出；
  - 云端：只接收 ingest 与提供查询；
  - Mac：开发与运维中控。
- **路径约定**：Windows 统一目录 `D:\market-live-terminal`。
- **数据流红线**：
  - 云端默认不主动外采（`ENABLE_CLOUD_COLLECTOR=false` for production）；
  - Windows 不作为 Git 主仓操作节点。
- **禁止行为（红线）**：
  - 在连接不稳定条件下进行无校验大文件覆盖；
  - 未核对文件完整性即执行云端 merge。

3. **实现摘要（当前方案）**
- 通过 Tailscale + SSH/SCP 远程驱动 Windows 脚本与 ETL；产物回传后在云端执行 merge。
- 目前已实现免密登录，但连接稳定性（active但不通）仍是短板。

4. **验收案例（Given / When / Then）**
- **正常**：Given Windows在线且SSH可达，When 执行 `sync_to_windows.sh`，Then 脚本同步成功且可远程触发 ETL。
- **边界**：Given 95MB 历史库通过 DERP 回传，When 传输完成，Then 本地文件大小/校验必须与源文件一致。
- **异常防线**：Given `tailscale status` 显示 active 但 `ping/ssh` 超时，When 触发流程，Then 任务必须进入阻塞待办而不是盲目继续。

5. **可观测性与告警**
- 观测点：`tailscale ping` 成功率、SSH连接成功率、大文件回传时延、Windows `sshd` 服务状态。
- 告警条件：连续连接失败、ETL产物回传中断、merge前校验失败。

6. **变更记录（任务ID）**
- `CHG-20260308-02`：Windows SSH 免密打通与脚本同步恢复。
- `CHG-20260309-03`：确认 ETL 已重建完成但回传/merge受连接抖动阻塞（登记 `T-002/T-003`）。
- `CHG-20260310-01`：生产 ingest 链路恢复（补齐 `akshare`、修正 `CLOUD_API_URL` 到 Nginx 入口、新增登录触发任务 `ZhangDataLiveCrawler`）。

---

### CAP-L2-HISTORY-FOUNDATION（盘后L2历史底座与回补机制）

1. **业务目标**
- 以“当日实时 + 盘后结算 + 次日正式历史”的方式，把每日盘后 L2 日包接入生产历史底座，统一服务盯盘页、日K资金博弈和未来复盘页。

2. **业务规则（强约束）**
- **历史最细粒度**：生产正式历史只落 `5m + 1d`，不落 `1m`。
- **多周期查询原则**：
  - `5m` 直接查正式底座；
  - `15m/30m/1h` 均由 `5m` 聚合；
  - 日线直接查日线正式表。
- **时间口径**：
  - 当日盘中：继续使用腾讯/实时链路；
  - 次日及以后：统一使用盘后 L2 正式历史值。
- **同源双派生**：
  - 同一份 L2 原始日包必须同时产出 L1 与 L2 派生结果；
  - L1=单笔阈值口径；L2=母单聚合口径；
  - 历史模块允许按需对比 L1/L2；
  - 截至 `2026-03-14`，盯盘页已新增“旧版 / 新版”整页切换；但新版最终形态已进一步冻结为：只保留 `当日分时` 与 `历史多维` 两个主区域，历史多维统一支持 `5m/30m/1h/日`。
- **目录规范**：
  - Windows 原始包统一放在 `D:\MarketData\YYYYMM\YYYYMMDD\{symbol}`；
  - 新下载的单日日包禁止长期裸放在 `D:\MarketData\YYYYMMDD` 根下。
- **写入红线**：
  - 云端不存原始 `逐笔成交/逐笔委托/行情`；
  - 同一 `symbol+trade_date` 的正式历史写入必须“整日删后重写”；
  - 禁止 append 式累加回补。
- **生产/实验分层**：
  - 正式底座在生产库；
  - sandbox 保留实验用途，不再作为长期正式页面数据源。
- **拟变更点（`CHG-20260315-03`）**：
  - 正式 `history_5m_l2 / history_daily_l2` 允许新增 `quality_info` 文本字段；
  - 无问题时为空，有问题时写一段人类可读说明；
  - 对完全缺失的日/5m 点，不强行写假值入正式表，而是在新版查询层补出 placeholder，并显式返回 `quality_info`。
- **拟变更点（`CHG-20260316-05`）**：
  - 若要把更多历史月份补进新版历史多维，优先按**固定股票池快照**逐月往前回补；
  - 推荐使用 sandbox V2 已验证的 `50~300亿、沪深A、排除ST` 池子快照，避免按当前市值动态漂移；
  - 对 `2026-02` 及更早月份，优先复用云端 `data/sandbox/review_v2/symbols/*.db::review_5m_bars`，按月提升到生产 `history_5m_l2 / history_daily_l2`；
  - 同步策略按“月度提升 -> 月度验收 -> 生产即时可用”执行，而不是一次性全历史全市场猛跑。
- **拟变更点（`CHG-20260316-07`）**：
  - 历史范围冻结为：**从 `2025-01-02` 起向后持续增量**；
  - `2025-01` 之前的更老月份不再纳入新版历史多维正式回补范围；
  - 后续正式流程收敛为：**Windows 下载日包 -> Mac 一条命令总控 -> 云端按日 merge 入正式库**。

3. **实现摘要（当前方案）**
- `2026-03-14` 已完成前置验真：Windows 上 `2026-03-11` 日包可读，且 `逐笔成交.csv::叫买/叫卖序号` 可与 `逐笔委托.csv::交易所委托号` 完整对齐。
- 截至 `2026-03-14` 当前已完成：
  - 正式表：`history_5m_l2`、`history_daily_l2`、回补状态表；
  - 预览表：`realtime_5m_preview`、`realtime_daily_preview` 已落 schema 与实时写透链路；
  - Windows 日级正式回补：支持强覆盖重跑；
  - 查询切换：盯盘历史 30m / 日K 历史分析已切到正式 L2 底座；
  - 历史日期分时回溯：已支持正式 `5m` L2 历史；
  - 前端来源标签：已改为显示接口真实 source；
  - 盯盘页已支持“旧版 / 新版”并存，且已验证过新版日线融合视图的视觉方向可行。
- 截至 `2026-03-16` 新增冻结：
  - `2025-01 ~ 2026-02` 固定池历史月份已全部提升到生产；
  - 后续不再继续往 `2025-01` 之前扩历史；
  - 日常只处理新增交易日，并由 Mac 一条命令完成“prepare -> 8 worker -> cloud merge -> 验真 -> 报告”。
- 当前后置项：
  - 新交易日盘后仍需按日执行“下载 → 清洗 → 正式入库 → 抽样验证”闭环；
  - 新版盯盘页虽然已完成“当日分时 + 历史多维”第一版，但仍缺真实大样本校正；
  - 复盘页并库仍未开始，按产品优先级后置；
  - Windows 裸日目录自动迁移脚本仍未完成；
  - 上证部分样本的母单号对齐策略还需继续观察。

4. **验收案例（Given / When / Then）**
- **正常**：Given `2026-03-17 10:00`，When 在盯盘页查看 `2026-03-14` 的历史数据，Then 应读取 `history_5m_l2` 的正式值，而不是当日实时缓存。
- **正常**：Given `2026-03-17 10:05`，When 在日K资金博弈模块查看 `2026-03-14`，Then 应返回 `history_daily_l2`，且可追溯该值来自盘后结算。
- **边界**：Given 补下载了 `2026-03-12` 的日包并重跑回补，When 查询该日历史，Then 结果应被正式覆盖更新而不是重复叠加。
- **异常防线**：Given 历史 L2 缺数，When 接口发生回退，Then 响应中必须显式携带 `fallback_used=true`，不得悄悄伪装成正式值。

5. **可观测性与告警**
- 观测点：
  - 某交易日是否完成正式结算；
  - 回补任务 `running/success/failed` 数量；
  - `history_5m_l2/history_daily_l2` 某日覆盖股票数；
  - fallback 命中率。
- 告警条件：
  - 新交易日盘后未完成正式回补；
  - 同一交易日重复写入导致唯一键冲突；
  - 历史查询 fallback 命中率异常升高。

6. **变更记录（任务ID）**
- `CHG-20260314-02`：完成单日日包格式验真，确认可做真实 L2 母单聚合。
- `CHG-20260314-03`：完成正式 L2 历史 schema 与目录规范落地。
- `CHG-20260314-04`：完成单日正式回补链路。
- `CHG-20260314-05`：完成历史查询切换、历史分时回溯接入与来源显示校正。
- `CHG-20260314-07`：新增盯盘页“旧版 / 新版”切换，并落地新版日线融合视图 V1（上价格区，下 L1/L2 绝对双柱）。
- `CHG-20260314-08`：冻结新版 IA 为“当日分时 + 历史多维”，并把 `2026-03` L2 数据入库后的盯盘/复盘同源方案写入文档。
- `CHG-20260315-01`：冻结历史多维副图扩展需求，规划在各粒度下新增“净流入对比 + 买卖力度分离”。
- `CHG-20260315-03`：冻结轻量质量提示方案，采用“正式表 `quality_info` + 查询层 placeholder + 前端统一异常样式”的最小改动路径。
- `CHG-20260316-01`：历史多维三张副图统一切换为真双柱渲染，并收紧 tooltip 边界与卡片布局；生产版本同步升级到 `v4.2.14`。
- `CHG-20260316-05`：完成 `2025-01 ~ 2026-02` 历史月份固定池提升到生产，历史多维正式范围扩展到 `2025-01-02` 起。
- `CHG-20260316-07`：冻结“后续只跑每日盘后新增日包”的正式流程，并新增 Mac 一条命令总控方案。
- `CHG-20260314-02`：完成新日日包单日格式验真，确认具备 L2 母单聚合前提。
- `CHG-20260314-03`：冻结总方案与四阶段实施卡，明确生产正式底座为 `5m + 1d`，并规定复盘页未来并库到生产 L2 底座。

---

### CAP-SANDBOX-REVIEW（L1/L2 复盘沙盒验证闭环）

1. **业务目标**
- 在完全隔离的沙盒链路中，复盘同一时间轴下 L1 与 L2 资金行为差异，并验证“母单还原”在妖股阶段的解释力。

2. **业务规则（强约束）**
- **隔离红线**：
  - 只读源 CSV/ZIP，不写生产 `market_data.db`；
  - API 仅新增 `/api/sandbox/*`；
  - 前端仅新增 `/sandbox-review` 页面与入口，不改现有业务图表口径。
  - 非生产发布策略固定为 `单分支 + Draft PR`，在数据就绪前禁止合并主干与生产部署。
- **时间粒度**：5 分钟切片（非交易时段不补零）。
- **L1 口径**：
  - 主力阈值：单笔 `amount >= 200,000`；
  - 超大单阈值：单笔 `amount >= 1,000,000`；
  - 输出绝对买入、绝对卖出、净流入。
- **L2 口径（母单还原）**：
  - 先按 `BuyOrderID`、`SaleOrderID` 日内汇总金额并打标；
  - 逐笔记账时，买方与卖方独立计入各自 buy/sell 侧；
  - 主力对倒不做过滤，允许上下柱同时放大，净流入单独反映方向性。
  - **严格模式**：缺失 `BuyOrderID/SaleOrderID` 列的源文件必须直接失败，禁止降级近似 L2。
- **金额口径**：
  - 优先使用源字段 `Amount/成交额`；
  - 缺失时依据 `Volume` 单位自动判定“股/手”后计算金额，并输出检测日志。
- **数据质量防线**：
  - 查询接口需检测“整日重复分时”异常（同一日内所有 5m OHLC 与资金字段与另一交易日完全一致）；
  - 默认剔除后出现的重复日，并在接口 `message` 返回异常日期对（如 `2026-02-23≈2026-02-11`）。

3. **实现摘要（当前方案）**
- 新增 sandbox ETL 脚本，输出独立库 `sandbox_review.db`（表：`review_5m_bars`）。
- 新增沙盒查询接口 `/api/sandbox/review_data`，返回标准 `APIResponse`。
- 新增前端页面 `/sandbox-review`：主图 K 线 + 资金多副图（绝对买卖、净流、净流比、锚点累计）。
- V2 升级为“可选股 + 可选时间 + 点击执行查询”：通过 `/api/sandbox/pool` 拉取股票池，`/api/sandbox/review_data` 按 symbol/date/granularity 查询。
- V2 存储升级为目录隔离：`data/sandbox/review_v2/`（`meta.db` + `symbols/{symbol}.db`），底层粒度固定为 5 分钟（不再维护 1 分钟持久层）。
- 页面交互：双端滑块横向拖动 + 快捷窗口（1/3/5/20/60日/全部），不使用滚轮缩放。
- 动态聚合：自动规则 `1日=5m, 3/5日=15m, 20日=60m, 60日/全部=1d`，并提供手动粒度覆盖（`5m/15m/30m/60m/1d`）。
- 前端可信度策略：Fail-Closed；接口失败或空数据时不回退本地预置数据，只展示错误/空状态。
- 统计验证扩展：新增相关性模块（`l1/l2 net` 与 `price_return/next_price_return` 的 Pearson）；活跃度口径使用 `total_amount`（该股票5分钟总成交额）并支持 `l2_activity_ratio>30%` 条件过滤。
- 锚点累计模式：点击 K 线设锚点后，累计区按 `主力L2/主力L1/超大L2/超大L1` 四图独立展示；每图采用“单累计曲线 + 正负面积分色”。
- 采用“先单日试跑，再全量刷数”的执行节奏。
- 全量长跑执行方式已升级为“全月份总控任务”：`sandbox_review_v2_run_all_months.py` 按 `2026-02 -> 2025-01` 逆序逐月自动串行，默认 `partial_done` 停机等待人工，全部完成后仅停在 `done` 态，不自动同步云端或发布版本。
- `2026-03-14` 已完成首轮全量闭环：Windows 全月份总控 `done`，云端已同步 `data/sandbox/review_v2/` 并重建前后端容器；当前 `/api/sandbox/pool` 返回池子 `2788`，`/api/sandbox/review_data` 可直接查询真实 5m 数据。

4. **验收案例（Given / When / Then）**
- **正常**：Given 查询窗口在 `2025-01-01 ~ 2026-02-28`，When 运行 sandbox ETL 或 V2 backfill，Then 对应 symbol 库包含目标区间 bars 且字段完整（OHLC + L1/L2 buy/sell/net）。
- **正常**：Given 请求 `/api/sandbox/review_data` 且参数合法，When sandbox 数据已准备，Then 返回 `code=200` 且时间升序数组（无补零）。
- **边界**：Given 某个源文件缺失 `BuyOrderID/SaleOrderID`，When 启动 ETL 严格模式，Then 任务必须失败并输出缺失文件清单。
- **异常防线**：Given 误配置输出路径指向 `market_data.db`，When 启动 ETL，Then 任务应拒绝执行并报错。

5. **可观测性与告警**
- 观测点：ETL 金额单位判定日志、`review_5m_bars` 行数、接口返回长度、前端六图联动表现（同一时间轴）。
- 告警条件：出现写入生产库风险、金额单位无法判定、单日试跑无有效 bars。

6. **变更记录（任务ID）**
- `CHG-20260310-02`：Sandbox L2 复盘 MVP（已完成本地实现与试跑验证，待真实源数据全量刷数）。
- `CHG-20260311-01`：完成 Windows `D:\\MarketData` 的 2026年1-2月真实全量刷数（1701 条），页面改为滑块拖动+动态聚合，默认展示真实区间。
- `CHG-20260311-03`：复盘页取消预置回退（Fail-Closed），并升级为自动+手动粒度切换（含 `60m/1d`）。
- `CHG-20260311-04`：复盘接口新增重复交易日自动剔除与404路由兼容探测，定位异常日 `2026-02-23≈2026-02-11`。
- `CHG-20260311-05`：新增 L1/L2 相关性与活跃度验证（total_amount 字段、统计脚本、散点图与统计卡片）。
- `CHG-20260311-06`：复盘页 V4 视觉重构为 6 图同屏；主力/超大柱图叠加 L1/L2 活跃度线，并将净流入拆分为主力/超大两张面积图，新增净流比图。
- `CHG-20260311-07`：复盘页新增“锚点累计模式”V1（点选K线设锚点），累计区升级为主力L2/主力L1/超大L2/超大L1四图，并统一为单曲线累计面积语义。
- `CHG-20260311-08`：完成沙盒复盘模块文档收口与非生产分支发布治理（Draft PR，明确 `NO-PROD` 与数据未就绪阻塞策略）。
- `CHG-20260311-09`：启动复盘模块 V2（云端可访问 + 生产隔离）：新增股票池接口、5m 底层分库存储、前端可选股可选时间查询入口与容量审计脚本。
- `CHG-20260311-09`（执行回填）：Windows 已固化股票池 2788 只、完成 `2025-01-01 ~ 2026-02-28` 全量回放，并将 `data/sandbox/review_v2/` 全量同步到云端；修复云端旧版 1m 查询模块后，sandbox API 已能返回真实复盘数据。
- `CHG-20260312-01`：根据最新业务决策取消实时L2前置，V2 回放改为 5m 直出；新增“自动分片+续跑+内存阈值动态并发”策略，提升全量跑数速度并降低人工干预。
- `CHG-20260312-04`：新增“全月份自动串行总控任务”，可一次启动后自动完成 `2026-02 -> 2025-01` 全段回放；任务完成后停在 `done`，等待页面校验与后续数据/版本发布决策。

---

## 4. 附录：统一术语

- **display_date**：前端展示日期，不等于自然日，必须服从 `CAP-MKT-TIME` 状态机。
- **回溯模式**：`display_date != 当日(北京时间)` 时的历史展示态。
- **污染窗口**：历史数据中存在明显占位或口径混杂、导致分析不可用的日期区间。
