# 07_PENDING_TODO（阻塞与待办）

> 只记录阻塞项、待办项与状态推进；不写长方案。

## T-001 Windows SSH 免密
- 状态：`DONE`（2026-03-08）
- 结果：`authorized_keys` 已配置，`sync_to_windows.sh` 可执行。
- 关联任务：`CHG-20260308-02`

## T-002 Windows ETL 产物回传与云端 merge
- 状态：`BLOCKED`
- 当前事实：
  - Windows `202602` ETL 已完成（15/15 DONE, 0 FAILED）。
  - 源文件：`D:\market-live-terminal\market_data_history_202602_fix.db`（约 95MB）。
  - 阻塞原因：Tailscale/SSH 连接抖动导致大文件回传中断。
- 下一步：
  1. 回传完整文件到 Mac（需校验大小一致）；
  2. 云端执行 `merge_historical_db.py`；
  3. 复核 `sz000833` 在 `2026-02-13` 的 30m 数据。
- 关联任务：`CHG-20260308-01`, `CHG-20260309-03`

## T-003 Windows 架构稳定性评估
- 状态：`TODO`
- 目标：评估“active但不通、DERP长传中断、睡眠后不可达”对生产流程的影响，并给出改造路线。
- 交付：`docs/10_WINDOWS_STABILITY_REVIEW_YYYY-MM-DD.md`（问题、根因、方案、优先级）。
- 关联任务：`CHG-20260309-03`

## T-004 协作约定：Windows链路改动前置检查
- 状态：`ACTIVE`
- 规则：凡涉及 `live_crawler_win.py`、`start_live_crawler.bat`、Windows ingest 调度改动，必须先检查 Windows 同步状态并记录在 handoff。

## T-005 Sandbox 复盘数据准备就绪（评审阻塞）
- 状态：`BLOCKED`
- 当前事实：
  - 复盘模块代码与文档已完成分支级收口，当前以 Draft PR 方式供评审与联调。
  - 真实效果验收仍依赖稳定的 sandbox 数据集（`sh603629` 2026-01~02）持续可复现。
- 解除条件：
  1. Windows 源目录 `D:\\MarketData` 可稳定重跑并产出一致的 `sandbox_review.db`；
  2. 本地 `/api/sandbox/review_data` 连续返回真实区间数据（无 404 / 无预置回退）；
  3. 复盘页主区与累计区在 1-2 月窗口通过一次回归冒烟。
- 关联任务：`CHG-20260311-08`

## T-006 Sandbox V2 股票池首构建（数据源短时断连）
- 状态：`DONE`（2026-03-11）
- 当前事实：
  - V2 代码与接口已就绪（`/api/sandbox/pool` + 1m 分库存储）。
  - 已新增 fallback（`stock_info_a_code_name/stock_zh_a_spot` + `stock_individual_info_em`）并在 Windows 成功落地。
- 结果回填：
  1. 股票池数量：`2788`（`as_of_date=2026-03-11`）；
  2. 冒烟回放：`run_id=1`，`symbol_count=20`，`total_rows=1317759`，`failed_count=0`；
  3. 当前样本占用：`data/sandbox` 约 `231.25MB`。
- 关联任务：`CHG-20260311-09`

## T-007 Sandbox V2 全量回放与云端可用打通
- 状态：`DONE`
- 当前事实：
  - Windows 已完成 `2025-01-01 ~ 2026-02-28` 全量 5m 回放，总控状态为 `done`；
  - 云端已完成 `data/sandbox/review_v2/` 首轮全量同步，`/api/sandbox/pool` 与 `/api/sandbox/review_data` 均可返回真实数据；
  - 已定位并修复“云端数据文件存在但接口空数组”的根因：容器内仍加载旧版 1m 查询模块，重建前后端容器后恢复正常。
- 解除条件：
  1. Windows 完成全量 `sandbox_review_v2_run_all_months.py`（一次启动、逐月逆序、按 `symbol+trade_date` 续跑），失败清单可追溯；
  2. `data/sandbox/review_v2/` 首轮同步到云端完成；
  3. 云端部署后 `/api/sandbox/pool` 与 `/api/sandbox/review_data` 可用，复盘页可正常查询。
- 结果回填：
  1. 股票池固定为 `2788`（`as_of_date=2026-03-11`），云端 symbol 分库首轮同步数为 `2789`（额外包含人工补跑样本 `sz000759`）；
  2. 云端目录 `data/sandbox/review_v2/` 占用约 `7.9G`；
  3. `sz000833/sz000759` 在 `2026-01-01 ~ 2026-02-28` 查询已可正常返回真实 5m 数据（接口层去重后为 `1666` 条）。
- 关联任务：`CHG-20260311-09`

## T-008 生产盯盘二态发布联动
- 状态：`ACTIVE`
- 当前事实：
  - 云端前后端已发布到 `v4.2.12`，其中 sandbox review 模块保持线上沙盒可用，周末“当日分时”回溯空白问题已修复。
  - Windows `live_crawler_win.py` 已同步到新的 `focus=5秒 / warm=30秒 / watchlist=15分钟` 逻辑，并于 `2026-03-14 14:05` 重启计划任务 `ZhangDataLiveCrawler` 生效。
  - 当前剩余的是前端体验层的人工确认：静默刷新时是否达到你主观上“丝滑、不闪”的标准。
- 解除条件：
  1. 生产实测确认实时页在周期刷新时无“先清空后回显”闪烁。
- 关联任务：`CHG-20260312-02`

## T-009 每日盘后 L2 日包融合方案（基础阶段）
- 状态：`DONE`（2026-03-14）
- 当前事实：
  - 已在 Windows 验证 `2026-03-11` 新日日包存在：`D:\\MarketData\\20260311\\20260311\\{symbol}`。
  - 抽样 `000833.SZ` 已确认三类文件齐全：`行情.csv`、`逐笔成交.csv`、`逐笔委托.csv`。
  - 样本成交文件包含 `叫买序号/叫卖序号`，且与委托文件 `交易所委托号` 可对齐，具备真实 L2 母单聚合前提。
  - `2026-03-14` 已冻结总方案：生产正式历史层固定为 `5m + daily`，并要求同源同时产出 L1/L2 双派生；复盘页未来并库到生产 L2 底座，sandbox 回归实验用途。
- 结果回填：
  1. Phase 1：`history_5m_l2/history_daily_l2/回补状态表` schema 与 `YYYYMM/YYYYMMDD` 目录 helper 已完成；
  2. Phase 2：`l2_daily_backfill.py` 已支持把盘后日包映射为 L1/L2 的 `5m + daily` 双派生并写入正式表；
  3. Phase 3：`/api/history/trend` 与 `/api/history_analysis` 已优先读取 `history_5m_l2/history_daily_l2`，并补充 `source/is_finalized/fallback_used`；
  4. Phase 3.5：历史日期回溯分时图已优先读取正式 `history_5m_l2`，`2026-03-11` 这类历史日期可直接在盯盘分时里看到 5m 的 L2 派生结果；
  5. 剩余产品化工作已拆分到新的待办 `T-010/T-011/T-012`，不再挂在本基础卡下。
- 关联任务：`CHG-20260314-02`

## T-010 2026年3月 L2 日包清洗与正式历史入库
- 状态：`ACTIVE`
- 当前事实：
  - 截至 `2026-03-15 10:40`，Windows `D:\\MarketData\\202603` 已下载并完成正式回补的交易日为：`20260302 / 03 / 04 / 05 / 06 / 09 / 10 / 11 / 12 / 13`；
  - 当前已下载日包已全部跑完，正式结果如下：
    - `03-02: daily=7731, 5m=347672`
    - `03-03: daily=7714, 5m=351291`
    - `03-04: daily=7724, 5m=347869`
    - `03-05: daily=7672, 5m=346077`
    - `03-06: daily=7644, 5m=343385`
    - `03-09: daily=5893, 5m=260777`
    - `03-10: daily=7348, 5m=331198`
    - `03-11: daily=7648, 5m=344439`
    - `03-12: daily=7630, 5m=344317`
    - `03-13: daily=7610, 5m=343162`
  - 当前缺口已收敛为：
    1. 明确 `OrderID` 对齐失败：原始批次为 `43` 个 `symbol-day`，已于 `2026-03-15` 全部定向修复；
    2. 无有效 bar / 无法形成正式 `5m+daily`：仍有 `105` 个 `symbol-day`
  - 稳定并发方案已确认：使用 **Mac 端多 SSH 会话 + 单日 8 worker shard**；Windows 本机父进程拉子进程方案仍不稳定。
  - `2026-03-15` 起，`l2_daily_backfill.py` 已改为把“无有效 bar”样本也写入 `l2_daily_ingest_failures`；但这 `105` 个历史样本仍需补录/复核。
  - `2026-03-15` Windows repair run `84~93` 已将 `43/43` 个显式 `OrderID` 失败样本补回正式表；当前 `2026-03-02 ~ 2026-03-13` 正式累计更新为：`history_daily_l2=74657`、`history_5m_l2=3361224`。
- 目标：
  1. 将 `2026-03` 已下载日包按统一目录规范整理；
  2. 逐日生成正式 `history_5m_l2/history_daily_l2`；
  3. 抽样验证盯盘页与后续复盘页所需的 5m/30m/1h/1d 数据覆盖。
- 解除条件：
  1. `2026-03` 已下载交易日完成正式回补；
  2. 至少完成 `sz000833` 等关键样本的多粒度抽样；
  3. 当前 `105` 个空结果样本完成补录/复核，确认哪些是真停牌/无成交，哪些需要重跑；
  4. 后续新增交易日继续按同一路径增量回补。
- 关联任务：`CHG-20260314-08`

## T-014 每日盘后 L2 正式回补自动编排固化
- 状态：`ACTIVE`
- 当前事实：
  - 当前真正稳定的正式路径是：**Windows 保存原始包与 staging，控制端通过多条 SSH 会话并发启动 8 个 worker**；
  - `backend/scripts/l2_day_sharded_backfill.py` 的 Windows 父进程 `Popen` 编排在 `2026-03-12` 首轮试跑中出现多组 `partial_done + 0 rows`，**问题定位为“当前这版 Python 父进程编排不稳定”**，而不是“Windows 本机永远不能自动跑正式回补”；
  - `docs/04_OPS_AND_DEV.md` 已在 `2026-03-16` 修订结论：Windows 端未来仍可独立定时完成盘后跑数，但要改为 **Task Scheduler / PowerShell / cmd 的 OS 级控制器 + 8 个独立 worker + DB 轮询验真**，不再沿用当前父进程 `Popen` 方案；
  - `docs/04_OPS_AND_DEV.md` 已冻结日常 Runbook：`16:15 检查日包 -> 16:20 解压 staging -> 16:25 8 worker 正式回补 -> 汇总失败 -> 清理 staging`。
  - `2026-03-16` 已新增 **Mac 一条命令总控** 第一版：`./ops/run_postclose_l2.sh`，可自动发现 pending day，并完成 prepare / 8 worker / cloud merge / 验真 / report；
  - `2026-03-16` 已完成首个真实单日演练：`20260316` 成功写入生产，结果为 `history_daily_l2=7663`、`history_5m_l2=345461`，latest merge run `id=102`、`status=partial_done`、失败样本 `15`；
  - 演练中已确认并修复三类编排问题：
    1. Windows worker 依赖未同步最新 `l2_history_db.py`；
    2. cloud merge 需要 `sudo` 写主库；
    3. cloud artifact 需用绝对路径，不能依赖 `~`。
  - 当前这张卡的剩余重点，已从“有没有一条命令”收敛为“如何把这条一条命令进一步做成完全无人值守的定时自动执行”。
- 目标：
  1. 保持 Mac 一条命令总控为当前正式日常入口；
  2. 若后续继续推进无人值守，再把同一语义迁移成 Windows 本机 OS 级定时控制器；
  3. 将 repair queue 导出也纳入每日固定动作。
- 解除条件：
  1. 每日盘后回补可无需人工挑日期、只需执行一次命令；
  2. 回补完成后可自动产出失败/空结果清单；
  3. staging 可自动清理，且原始 `.7z` 继续保留；
  4. 若要继续升级为全自动，则需再完成定时触发与跨重启稳定性验证。
- 关联任务：`CHG-20260315-02`

## T-015 新版历史多维历史月份扩展（固定池、按月回补、按月上线）
- 状态：`DONE`（2026-03-16）
- 当前事实：
  - 新版历史多维当前正式覆盖主要集中在 `2026-03`；
  - sandbox V2 已沉淀固定股票池：`50~300亿市值、沪深A、排除 ST`，当前池子规模约 `2788`；
  - 对 `2026-02` 及更早月份，首选路径已收敛为：**直接复用 sandbox V2 的固定池 5m 产物，按月提升到生产 `history_5m_l2/history_daily_l2`**，不先重跑 Windows 原始 ZIP；
  - 已新增月度提升脚本：`promote_sandbox_review_v2_month.py`、`run_l2_history_monthly_rollout.py`；
  - 云端月度 rollout 已完成：`2026-02 -> 2025-01` 全部 done；
  - 用户已明确冻结范围：**`2025-01` 之前不再继续回补**。
- 结果回填：
  1. 历史多维正式覆盖已扩展到 `2025-01-02 ~ 2026-03-13`；
  2. `2025-01` 之前的更老月份不再继续纳入本项目范围；
  3. 后续工作重心已切换为“每天盘后新增日包的日增量回补”。
- 关联任务：`CHG-20260316-05`

## T-016 Windows 实时采集计划任务稳态化
- 状态：`ACTIVE`
- 当前事实：
  - `2026-03-16` 盘中生产盯盘页出现“无实时 K 线”；
  - 已确认根因不是前端，而是 Windows 计划任务 `ZhangDataLiveCrawler` 未自动拉起；
  - 现场排查时，这条任务的 `上次运行时间` 停在 `2026-03-14 14:05:32`，`上次结果=-1073741510`；
  - `2026-03-18` 已完成第二阶段稳态化：正式任务改为 `SYSTEM + 开机触发 + 每5分钟重复 + IgnoreNew`，旧 watchdog 任务已删除；
  - 同步治理前 Windows 上曾堆积 `11` 个重复 `python.exe` crawler；重建任务后已收敛为 `1` 个，并跨下一个 5 分钟周期复检仍保持单实例；
  - 生产 `/api/realtime/dashboard?symbol=sz000833` 与云端 `trade_ticks/sentiment_snapshots` 已恢复 `2026-03-18` 当天真实数据。
- 目标：
  1. 将 `ZhangDataLiveCrawler` 改造成可跨重启、注销、周末后首个交易日稳定拉起的正式任务；
  2. 明确触发策略、失败重试策略与盘中健康检查；
  3. 在 Runbook 中冻结“异常先查计划任务，再查前端”的诊断顺序。
- 解除条件：
  1. 任务在非交互场景下也可稳定启动；
  2. 至少完成一次“重启或注销后自动恢复”的实测；
  3. 生产盘中抽样确认 `/api/realtime/dashboard` 数据连续更新，无需人工 `schtasks /Run` 干预。
- 下一步：
  1. 补做一次 Windows 重启或注销后的自动恢复演练；
  2. 如需更强验真，再为 crawler 增加 heartbeat/成功轮询日志；
  3. 演练通过后，将本卡从 `ACTIVE` 改为 `DONE`。
- 关联任务：`CHG-20260316-06`, `CHG-20260318-01`

## T-011 盯盘页新版信息架构收敛（当日分时 + 历史多维）
- 状态：`ACTIVE`
- 当前事实：
  - 截至 `2026-03-15`，新版前端 IA 第一版已落地：`旧版 / 新版` 并存，新版内部已收敛为 `当日分时 + 历史多维`；
  - 历史多维已支持 `5m / 30m / 1h / 日` 切换，并已接入 `/api/history/multiframe`；
  - 当前剩余工作不是结构切换本身，而是等待 `2026-03` 正式数据到位后继续做大样本校正、空态与交互打磨。
- 目标：
  1. 完成新版盯盘页 IA（tab/segment 切换方式）定稿；
  2. 历史多维模块统一消费正式 L2 底座，盘中可按规则显示 preview/finalized；
  3. 旧版保留，新版逐步替换旧 `30分钟线 + 日线` 组合。
- 关联任务：`CHG-20260314-08`

## T-013 历史多维副图扩展：净流入对比 + 买卖力度分离
- 状态：`ACTIVE`
- 当前事实：
  - 截至 `2026-03-15`，历史多维已完成第一版副图接入：主图下方已新增“净流入对比 + 买卖力度分离”两张副图；
  - 你新确认的需求是：把老版日线里的“主力净流入”和“买卖力度分离”两张图，按新版 `L1 vs L2`、`主力 vs 超大单` 思路重新设计；
  - 当前实现已覆盖 `5m / 30m / 1h / 日` 四个维度，但仍缺 `2026-03` 正式全月数据做大样本校正。
- 目标：
  1. 冻结两张副图的统一视觉语义与计算公式；
  2. 首期尽量复用 `/api/history/multiframe` 现有字段，优先走前端派生，不先新增后端接口；
  3. 待 `2026-03` 正式数据到位后，继续做默认视窗、tooltip 密度与空态校正。
- 阻塞：
  1. `2026-03` 全月正式 L2 数据尚未下载完并入库，当前缺少足够样本校正新副图；
  2. 各粒度默认视窗、tooltip 密度、空态策略仍需依赖真实数据调优。
- 关联任务：`CHG-20260315-01`

## T-012 复盘页正式底座并库与 sandbox 角色重定义
- 状态：`TODO`
- 当前事实：
  - sandbox review v2 已能稳定提供 `2025-01 ~ 2026-02` 的实验数据；
  - 但复盘页正式查询尚未切到生产 `history_5m_l2/history_daily_l2`。
- 目标：
  1. 明确复盘页正式查询层与 sandbox 实验层边界；
  2. 在 `2026-03` 数据入库后，规划并执行复盘页正式并库；
  3. sandbox 保留为“实验/验证/特殊研究”，不再承担长期正式展示。
- 关联任务：`CHG-20260314-06`, `CHG-20260314-08`


## T-017 当日分时页 L1/L2 双轨重构后置项
- 状态：`ACTIVE`
- 当前事实：
  - `2026-03-19` 已完成从文档冻结到首轮实现：`STG-20260318-03` + `REQ-20260318-03~06` 已对应落地；
  - 开发分支 `codex/funds-battle-l1-l2-dual-track` 已创建并完成首轮开发验证；
  - 当前剩余的仅是已知后置项，需继续登记避免再次口头悬空。
- 后置项：
  1. 调查 `逐笔委托::委托类型=U` 是否需要从 cancel 独立拆分；
  2. 当前调参 UI 已按股票 `symbol` 持久化到浏览器本地；若后续需要跨设备同步或写入全局配置，需另开卡冻结权限与作用域。
  3. 复盘页是否复用 `intraday_fusion` 统一双轨接口，待当前页稳定后再定。
  4. 已存在的旧版 finalized 日（如本地刚同步验证的 `2026-03-18` 样本）可能只有 L2 买卖事实，缺少 `add/cancel/l2_cvd_delta/l2_oib_delta` 扩展因子；若要让新信号引擎完整生效，需补一次历史字段级重算。
- 关联任务：`CHG-20260319-01`
