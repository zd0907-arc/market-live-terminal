# MOD-20260421-01 项目当前状态与文档治理归一

## 1. 背景
- 2026-04 期间，项目并行推进了：
  - 本地研究站 / Windows 数据主站；
  - atomic 数据治理；
  - 正式复盘并库；
  - 选股研究工作台；
  - 单票新闻 / 公告 / 互动问答事件层。
- 在多 worktree、多分支、多轮 AI 会话并行后，仓库出现了两个问题：
  1. **代码当前真相** 与 **core docs 描述** 漂移；
  2. `docs/changes/` 中沉淀了大量过程卡，缺少一个“现在到底什么是真的”的统一入口。

## 2. 当前真相（2026-04-21）

### 2.1 主代码与工作目录
- 当前唯一主工作目录：`/Users/dong/Desktop/AIGC/market-live-terminal`
- 当前唯一主线分支：`main`
- 当前主线已包含：
  - 本地研究站口径；
  - 正式复盘主链路；
  - 选股研究工作台；
  - 官方事件层（`stock_events`）。

### 2.2 当前页面入口
- 首页：只保留新版壳层，不再保留“旧版 / 新版”切换作为当前真相描述。
- 正式复盘页 canonical 路由：`/review`
- 历史兼容路由：`/sandbox-review`
  - 当前仅作为兼容入口；
  - 前端会自动收口到 `/review`。
- 选股研究页：`/selection-research`

### 2.3 当前三端职责
- Windows：raw、正式跑数、重型数据真相源；
- Mac：当前唯一开发 / 复盘 / 选股 / 文档主工作台；
- Cloud：轻量盯盘 / 应急查看。

### 2.4 当前文档治理原则
- `docs/02~08` 只保留**当前真相 / 当前规则 / 当前待办**；
- `docs/changes/*.md` 保留历史过程，但不再充当“当前真相入口”；
- 若一个主题已有大量 `REQ/STG/INV/MOD` 过程卡，优先通过一张 `MOD-* current-state` 母卡收口；
- 历史卡不删，但需要让读者一眼知道“先看哪一张”。

## 3. 这次治理收口的目标
1. 修正 core docs 中与当前代码不一致的描述；
2. 把过期的“拟变更 / 尚未合并 / 旧 worktree”说法改成当前已完成状态；
3. 给当前真相增加单一入口，减少后续 AI 会话误读；
4. 对 `07_PENDING_TODO` 做最小压缩：
   - 保留未完成项；
   - 已完成但仍挂 `ACTIVE` 的，改成完成或移出当前主视角。

## 4. 本次治理后读文档的顺序
1. 当前真相：本卡
2. 业务真相：`docs/02_BUSINESS_DOMAIN.md`
3. 数据/API 真相：`docs/03_DATA_CONTRACTS.md`
4. 运行/启动真相：`docs/04_OPS_AND_DEV.md`
5. 快速入口：`docs/AI_QUICK_START.md`
6. 未完成事项：`docs/07_PENDING_TODO.md`

## 5. 当前仍未完成的主题（不在本卡假装完成）
- `stock_events` 的无 Token 公共源仍未补齐 `QA / media news`；
- `stock_universe_meta` 自动刷新仍未正式调度；
- 存量旧表依赖剥离仍未完成；
- 部分历史数据治理卡仍是过程性设计文档，尚未全部转为单一最终态说明。

## 6. 风险
- 如果后续继续直接阅读早期 `REQ/STG` 卡而不先看本卡，仍可能误把历史计划当当前真相；
- 当前仓库仍保留大量历史过程卡，信息量大，但已经通过本卡显式把“当前入口”固定下来。
