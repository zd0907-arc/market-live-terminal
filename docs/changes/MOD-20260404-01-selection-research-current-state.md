# MOD-20260404-01-selection-research-current-state

## 1. 基本信息
- 标题：选股研究工作台｜V2 当前真实状态
- 状态：DONE
- 负责人：Codex
- 关联 CAP：`CAP-SELECTION-RESEARCH`
- 关联 Task ID：`CHG-20260404-01`

## 2. 当前真实状态
- 工作台定位已从“研究数表页”调整为：
  - 左侧 `Top10` 启动确认候选池
  - 右侧复盘决策视图
- `stealth` 保留为内部前置与解释信号
- `distribution` 保留为能力，但不再做全市场榜单，只对当前票输出风险判断
- 右侧复盘区以复用现有成熟组件为主，不重造独立复盘页
- 案例反推不做功能入口，仅保留为文档化研究分支

## 3. 这次实现落地了什么
- `/api/selection/candidates` 默认改为 breakout Top10
- 候选默认范围已收敛为沪深A（`sh60* / sz00* / sz30*`），先排除科创板/北交所
- 候选列表改为精简卡片：排名 / 名称+代码 / 综合分 / 标签 / 一句话原因
- 右侧顶部判断区已从纵向宽列改为横向摘要，主图区域扩大
- 新增信号后窗口控制：`40天 / 90天 / 到现在 + 起止日期`
- 右侧新增并整合：
  - 当前综合判断
  - 为什么选中它
  - 出货风险判断
  - 事件时间线
- 右侧主图优先改为 `HistoryMultiframeFusionView`，直接看价格/L1/L2 资金绝对值/净流入/买卖力度/活跃度线
- 右侧图表已切到选股专用数据通道：本地优先，缺数据时自动回退云端只读 `/api/history/multiframe`
- 若某票暂无正式 L2，再回退 `HistoryView(local_history)` 做日级资金补充
- 回测升级为双口径：
  - 固定持有收益（原口径）
  - 持有窗口内最高机会（`max_runup_within_holding_pct`）

## 4. 当前限制
- `stock_universe_meta` 为空，正式名称映射未补齐；当前页面通过前端 fallback 尽量显示名称
- 动态出货退出回测未做；当前只完成“固定持有 + 窗口机会”双口径
- `2026-03+` 全市场连续覆盖仍不完整
- 本地 `history_daily_l2/history_5m_l2` 只覆盖极少数 symbol，所以右侧完整 L2 复盘仍依赖后续补数
- 已核验 Windows `2026-03-02 ~ 2026-03-13` 正式 L2 覆盖为 `8,111` symbols，但这批数据还没同步回本地
- 当前云端 fallback 只用于选股模块右侧，不会改坏原首页/复盘页的数据读取链路

## 5. 相比旧版规划的核心调整
- 不再把三策略做成平权主入口；左侧只保留 breakout 候选池
- 不再新造右侧五块复盘模块；改为复用现有复盘成熟能力，再额外补判断块
- 出货风险从“全市场榜单”改成“当前票风险识别能力”
- 40日评估不再只看第40天收益，增加窗口内最高机会口径
- 案例反推从“拟做功能入口”改成“只做对话研究与文档沉淀”

## 6. 下一步
1. 补正式名称映射数据
2. 对齐本地正式 `history_daily_l2/history_5m_l2`
3. 长窗口回测补齐 `5/10/20/40`
4. 二期再评估动态出货退出回测
