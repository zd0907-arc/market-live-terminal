# 星火 v2 次日退出策略验证

## 状态

保留。

这是退出策略验证，不是新候选模型。

## 当前判断

- 用来判断次日盘中 / 收盘退出规则对已有候选是否有帮助
- 目前仍属于验证层

## 入口文件

- `policy_summary.csv`

## 配套结果

- `stable_policy_trades.csv`
- `offensive_policy_trades.csv`
- `run_summary.json`
