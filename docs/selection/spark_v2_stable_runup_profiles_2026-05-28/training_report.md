# 星火 v2 稳定冲高训练总报告

## 结论

稳健型候选：`stable_003_hit20_full`，含义是“预测 22 日至少冲高 20%，全量特征”。
进攻型参考：`pure_001_rank_full`，含义是“每日全市场相对冲高排序，全量特征”。

稳健型在 2026 最终检验里，买后从未上涨比例是 `1.56%`，5%/10%/15%/20% 冲高概率分别是 `70.31%` / `53.12%` / `35.94%` / `26.56%`，连 5% 都没冲到的比例是 `29.69%`，典型冲高水平中位数是 `12.01%`。
进攻型参考在 2026 最终检验里，买后从未上涨比例是 `0.00%`，5%/10%/15%/20% 冲高概率分别是 `84.38%` / `68.75%` / `57.81%` / `50.00%`，连 5% 都没冲到的比例是 `15.62%`，典型冲高水平中位数是 `19.93%`。

## 市场状态判断

- 作为特征：市场环境特征组里最好的候选分数是 `-22.381`，能证明它有信息量，但还不能单独打赢全量特征。
- 作为分层：强/弱市场下的 `market_regime_metrics.csv` 已输出。
- 作为闸门：弱市场切换到稳健型后，2026 的 5%/10%/15% 冲高概率是 `84.38%` / `67.19%` / `51.56%`，买后从未上涨比例 `0.00%`；如果弱市场直接少做，过滤后样本里的 5%/10%/15% 冲高概率是 `84.91%` / `67.92%` / `56.60%`。

## 业务判断

当前稳健型候选是可用的，它确实更偏向于降低买到顶峰以后完全不涨的情况，但进攻型参考在大幅冲高能力上仍然更强。市场状态信息有帮助，尤其适合做分层和闸门，但现阶段还不足以单独替代原始强势排序。

## 约束

- 2026 没有进入训练、调参或选模。
- 不做卖点、仓位、资金复利和盘中交易。
