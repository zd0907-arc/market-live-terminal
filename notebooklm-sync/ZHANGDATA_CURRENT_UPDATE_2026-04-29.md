# ZhangData Current Update 2026-04-29

Generated: 2026-04-29

## 当前版本与工作区

- 当前工作版本：`v5.1.0`
- 当前工作目录：`/Users/dong/Desktop/AIGC/market-live-terminal`
- 当前分支：`codex/market-heat-sensing`
- 另有 worktree：`/Users/dong/Desktop/AIGC/market-live-terminal-selection-research-context`

## 本期核心判断

当前已落地模块仍是三条：

```text
盯盘
正式复盘
选股研究工作台
```

热点板块 / 市场热度研究正在推进，但仍是探索能力，不进入正式模块口径。

## 盯盘当前状态

盯盘主链路已经稳定在：

```text
Windows 实时 crawler
-> Cloud 轻量 ingest
-> 前端实时盯盘
```

Mac 本地研究站默认不长期跑生产级 crawler，只读取同步库并支持单票按需 hydrate。

仍需观察：

- Windows 实时采集计划任务稳态化；
- tick 单源风险与 fallback；
- 自然日运行后是否再次出现错库或错进程。

## 正式复盘当前状态

正式复盘 canonical 路由是 `/review`。

后端接口：

```text
/api/review/pool
/api/review/data
```

支持：

```text
5m / 15m / 30m / 60m / 1d
```

仍需继续：

- 本地正式历史覆盖补齐；
- 旧兼容链路收口；
- 存量表依赖剥离验证。

## 选股研究当前状态

当前产品已经从单策略页推进到每日复盘决策页。

已接入：

- 资金流回调稳健；
- 趋势中继高质量回踩；
- 每日复盘决策；
- 策略节点打点；
- 策略解释浮窗。

当前保守判断：

```text
选股模块已经能支持研究和观察，
但策略表现、覆盖面和真实可交易性还没有达到稳定自动买入信号标准。
```

资金流回调稳健：

- 质量较高；
- 覆盖面窄；
- 适合继续模拟盘和观察盘验证。

趋势中继高质量回踩：

- 已接入页面；
- 样本少；
- 不能把少量成熟样本当成充分稳定结论。

热点共振：

```text
breakout + 热点共振当前验证偏负。
热点不能直接作为选股加分项。
```

## 热点板块研究当前状态

热点研究当前结论：

```text
热点板块能解释强股更容易从哪里来，
但不能直接产生可买收益。
```

比较确定：

- 细颗粒板块口径是必要的，大主题太粗；
- 细颗粒 Top15 + 日内共振能把覆盖率压到约 5%；
- 热点对未来强股有解释力，Lift 长期大于 1；
- 热点池不能单独买，Alpha 长期不转正；
- L2 过滤能提高 Lift，但不能改善收益；
- breakout + 热点共振偏负；
- breakout + climax_hot 是风险提示。

当前建议：

- 不继续把热点当收益来源调参；
- 先作为解释标签和风险提示；
- 后续可继续研究“热点内低位 L2 潜伏”。

## 当前待办重点

- `T-014`：每日盘后 L2 正式回补自动编排固化；
- `T-016`：Windows 实时采集计划任务稳态化；
- `T-019`：原子事实层全链路 30 分钟目标；
- `T-020`：本地研究站稳定性观察；
- `T-022`：选股工作台数据对齐 + 策略效果滚动验证；
- `T-026`：原子事实层主路径后续收口；
- `T-027`：单票新闻 / 公告 / 互动问答事件理解层；
- `T-031`：存量表依赖剥离验证。

## 本期文档整理动作

- 核心文档版本口径对齐到 `v5.1.0`；
- 三大已落地模块边界重新回写；
- 选股策略风险边界回写；
- 热点板块标为 `EXPLORING`；
- `AI_HANDOFF_LOG` 压缩旧窗口；
- 建立独立的 NotebookLM 同步产物区 `notebooklm-sync/`。

## NotebookLM 操作建议

本文件属于日期更新包。

下次同步时：

```text
1. 删除 NotebookLM 中旧的 ZHANGDATA_CURRENT_UPDATE_YYYY-MM-DD.md
2. 上传新的日期更新包
3. 保留 ZHANGDATA_STABLE_CONTEXT_v5.1.md，除非项目形态发生大变化
```
